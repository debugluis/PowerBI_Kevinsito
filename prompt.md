# Prompt para generar un nuevo dashboard

Copia y adapta esto:

---

Este programa genera un dashboard en formato PBIP desde datos en Python.

Quiero que generes un nuevo dashboard basado en el archivo "[archivo]". [Si los datos necesitan limpieza, describir que problemas tienen]. El dashboard debe tener filtros por [columnas], tarjetas KPI de [medidas], un grafico de [tipo] con [que mostrar y como ordenar], un [otro grafico] con [que mostrar], y una tabla con [columnas y medidas, que excluir, como ordenar].

Manten toda la infraestructura de exportacion PBIP para no afectar compatibilidad. Explicame que vas a hacer antes de implementar.